---
title: "Series of functions; the Weierstrass M-test"
date: 2019-02-24T17:05:00+08:00
volumes: ["10"]
layout: "note"
type: "notes"
issue: 5
weight: 105

---

<!--more-->
<div class="latex-macros">
  {{< raw >}}
    $\newcommand{\supr}{\text{sup}}$
    $\newcommand{\infi}{\text{inf}}$
  {{< /raw >}}
</div>
{{% definition name="Finite sum of real-valued functions" status="" %}}

$(f^{(n)})^N\_{ n =1} : X \to \R$, we define the finite sum $\sum\_{ i=1 }^{ N } f^{(i)} : X \to \R $ by

$$(\sum\_{ i=1 }^{ N } f^{(i)}) (x) \triangleq \sum\_{ i=1 }^{ N } f^{(i)}(x)$$

{{% /definition %}}

{{% definition name="Infinite Series" status="" %}}

$(X, d\_X), (f^{(n)})^\infty\_{ n =1} ,f : X \to \R.$

If the partial sums $\sum\_{ n = 1 }^{ N} f^{(n)} $ converge pointwise to $f$ on $X$ as $N \to \infty$, we define the infinite series $\sum\_{ n=1 }^{ \infty } f^{(n)} $ converges pointwise to $f $, and write $f = \sum\_{ n=1 }^{ \infty } f^{(n)}$.

If the partial sums $\sum\_{ n=1 }^{ N } f^{(n)}$ converge uniformly to $f$ 
on $X $ as $N \to \infty $, we say that the infinite series $\sum\_{ n=1 }^{ N} f^{(n)} $ converge uniformly to $f $ on $X$ as $N \to \infty$, we say that the inifite series $\sum\_{ n=1 }^{ \infty } f^{(n)} $ converges uniformly to $f $, and again write $f = \sum\_{ n=1 }^{ \infty} f^{(n)}$.

{{% /definition %}}

{{% remarks name="" %}}

A series $\sum\_{ n=1 }^{ \infty } f^{(n)} $ converges pointwise to $f $ on $X $ if and only if $\sum\_{ n=1 }^{ \infty } f^{(n)}(x) $ converges to $f(x) $ for every $x \in X $.

{{% /remarks %}}

{{% theorem name="" index="" status="" %}}

$(X, d) $, $(\R, d\_{l^2}) $.

If $(f^{(k)})^\infty\_{ k =1} : X \to \R$ is a collection of continuous bounded functions such that $\sum\_{ k = 1 }^{\infty} \norm{f^{(k)}}\_ \infty $ converges. Then there exists a continuous bounded function $f : X \to \R $ such that $\sum\_{k = 1}^{\infty} f^{(k)}$ converges uniformly to $f$.

{{% /theorem %}}


